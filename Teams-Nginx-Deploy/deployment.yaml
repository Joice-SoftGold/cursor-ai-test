# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-from-chatops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-chatops
  template:
    metadata:
      labels:
        app: nginx-chatops
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
