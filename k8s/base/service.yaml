apiVersion: v1
kind: Service
metadata:
  name: gitops-webapp
  namespace: gitops-webapp
  labels:
    app.kubernetes.io/name: gitops-webapp
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: gitops-webapp
